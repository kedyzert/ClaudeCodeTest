# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project state

This repository is currently empty scaffolding — a workspace with no application code, build system, or tests yet. There are no commands to build, lint, or test because no language/framework has been chosen. When code is added, update this file with the actual commands (build/lint/test/run) and the resulting architecture.

## Conventions

- All work in this project should be version-controlled with Git and backed up to GitHub automatically (commit and push as work is done, per the user's standing preference).
